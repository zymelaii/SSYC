int f(int x, int y) {
    return
        /* section 1 */
        6 % y +
        /* section 2 */
        x + x + x + x + x + x + x +
        /* section 3 */
        y * x * y * 888 * x * 123 * y * y * y * x
        /* section 4 */
        - 9;
}

int main() {
    return f(0, 1);
}
